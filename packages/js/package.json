{
	"name": "@fatima.dev/js",
	"description": "JavaScript bindings for the Fatima native CLI",
	"version": "0.0.0",
	"license": "MIT",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build:wasm": "cargo run -p task -- build-binaries --target wasm",
		"copy:wasm": "mkdir -p dist/wasm && cp ../../.release/raw/wasm-pack/fatima_wasm.js ../../.release/raw/wasm-pack/fatima_wasm.d.ts ../../.release/raw/wasm/fatima_wasm_bg.wasm dist/wasm/",
		"build:prebuilt": "tsc -p tsconfig.build.json --declaration --emitDeclarationOnly false --outDir dist --declarationMap false && pnpm run copy:wasm",
		"build": "pnpm run build:wasm && pnpm run build:prebuilt",
		"checktypes": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "24.9.1",
		"typescript": "6.0.3"
	}
}
