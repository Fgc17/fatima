{
	"name": "@fatima.dev/js",
	"version": "0.0.1",
	"description": "JavaScript bindings for the Fatima native CLI",
	"license": "MIT",
	"files": [
		"dist"
	],
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build:wasm": "cargo run -p task -- build-binaries --target wasm",
		"copy:wasm": "mkdir -p dist/wasm && cp ../../.release/raw/wasm-pack/fatima_wasm.js ../../.release/raw/wasm-pack/fatima_wasm.d.ts ../../.release/raw/wasm/fatima_wasm_bg.wasm dist/wasm/",
		"build:prebuilt": "tsc -p tsconfig.build.json --declaration --emitDeclarationOnly false --outDir dist --declarationMap false && bun run copy:wasm",
		"build": "bun run build:wasm && bun run build:prebuilt",
		"checktypes": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "24.9.1",
		"typescript": "6.0.3"
	}
}
