```js
const { providers } = require("fatima");
const { InfisicalSDK } = require("@infisical/sdk");

module.exports = config({
  providers: {
    development: [providers.local(".env"), providers.infisical(InfisicalSDK)],
  },
});
```
