```ts
import { providers } from "fatima";
import { InfisicalSDK } from "@infisical/sdk";

export default config({
  providers: {
    development: [providers.local(".env"), providers.infisical(InfisicalSDK)],
  },
});
```
