```ts
import { adapters } from "fatima";
import * as trigger from "@trigger.dev/sdk/v3";

export default config({
  load: {
    development: [adapters.local.load(".env"), adapters.trigger.load(trigger)],
  },
});
```
