engine marzano(0.1)
language js

or {
    `process.env` as $match where {
        register_diagnostic(
            span = $match,
            message = "Access to the 'process.env' object is not allowed, use 'env' or 'publicEnv'.",
            severity = "error"
        )
    }
}
