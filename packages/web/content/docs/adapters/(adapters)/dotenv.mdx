---
title: dotenv
description: '"Dotenv is a zero-dependency module that loads environment variables from a .env file into process.env"'
---

This loader is a dumb wrapper around `dotenv`, it is useful to load envs from local files.

## Dependencies

<Tabs groupId='package-manager' persist items={['npm', 'pnpm', 'yarn']}>

```bash tab="npm"
npm install dotenv
```

```bash tab="pnpm"
pnpm add dotenv
```

```bash tab="yarn"
yarn add dotenv
```

</Tabs>

## Importing the loader

```ts title="env.config.ts"
import { config, providers } from "fatima";
import dotenv from "dotenv";

export default config({
  providers: {
    development: providers.dotenv(dotenv),
  },
});
```
