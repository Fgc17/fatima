---
title: local
description: Load environment variables from local .env files with no dependencies.
order: 2
---

This provider loads environment variables from `.env` files with no dependencies.

## Dependencies

Zero.

## Configuring the provider

```json
{
  "providers": {
    "development": [
      { "provider": "local", "files": [".env", ".env.local"] }
    ]
  }
}
```

## Special thanks

Special thanks to [dotenv](https://github.com/motdotla/dotenv) for the regex that reads `.env` lines, [source code can be found here.](https://github.com/motdotla/dotenv/blob/master/lib/main.js)
