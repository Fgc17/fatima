[package]
name = "task"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
inquire.workspace = true
time.workspace = true
zip = { version = "0.6", default-features = false, features = ["deflate"] }
